version: "3.8"
services:
  mavoid-portfolio:
    build:
      context: .
      dockerfile: Dockerfile
    image: mavoid-portfolio:latest
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - VITE_ADMIN_PASSKEY=mavoid2026
      - VITE_FIREBASE_API_KEY=AIzaSyDbN-Yp85RAvg56pAPUNBrYBRMa8Bn36Nc
      - VITE_FIREBASE_APP_ID=1:346980739163:web:1d3c2ee78960a2da56377e
      - VITE_FIREBASE_MEASUREMENT_ID=G-K5FN7J6WNH
      - VITE_FIREBASE_MESSAGING_SENDER_ID=346980739163
    # Bind to localhost so host nginx can proxy to it without exposing publicly
    ports:
      - "3193:80"
    networks:
      - web
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost/" ]
      interval: 30s
      timeout: 10s
      retries: 3

networks:
  web:
    driver: bridge
